apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ceph-data
spec:
  storageClassName: rook-cephfs
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 2000G

---

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ceph-experiments
spec:
  storageClassName: rook-cephfs
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 100G